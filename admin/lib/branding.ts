// F25 (GK-468). PLACEHOLDER — NOT the final client-facing name.
//
// "membership_saas" is our internal repository codename. It was showing to
// Grant and the curators in four places: the browser tab, the login card, the
// sidebar header and the dashboard subtitle. Naming the product is the client's
// decision, not an agent's, so this is deliberately a neutral descriptor rather
// than an invented brand.
//
// TODO (Murad): ask Grant what this panel should be called, then replace this
// one constant — every user-visible occurrence reads from here.
export const PANEL_NAME_PLACEHOLDER = "Админ-панель";
